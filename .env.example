# Server only. Jangan pakai prefix VITE_, supaya kunci tidak masuk bundle frontend.
GEMINI_API_KEY=

# Opsional. Default: gemini-2.5-flash
# GEMINI_MODEL=gemini-2.5-flash

# Deploy: set GEMINI_API_KEY di Environment Variables Vercel.
# Di Vercel Firewall, batasi POST /api/review, misalnya 10 permintaan per 10 menit per IP.
